---
import { Image } from "astro:assets";
import { } from '@lucide/astro';
import astro from '../assets/astro.png'
import vue from '../assets/vue.png'
import next from '../assets/next.png'


---
<section
  id="tecnologias"
  class="py-16 px-6 bg-gradient-to-b from-[#0B0F25] to-[#1B1140] text-white"
>
  <div class="max-w-6xl mx-auto text-center">
    <!-- Título con gradiente en el texto -->
    <h2
      class="text-3xl md:text-4xl font-extrabold bg-clip-text text-transparent
             bg-gradient-to-r from-cyan-400 to-purple-500 mb-2"
    >
      Tecnologías Nativas
    </h2>
    <p class="text-gray-300 mb-12">
      Trabajamos con las tecnologías más avanzadas para garantizar el máximo rendimiento
    </p>

    <!-- Grid responsivo para 3 tarjetas -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <!-- Tarjeta Astro.js -->
      <div
        class="flex flex-col items-center bg-white/10 backdrop-blur-md rounded-xl
               p-6 hover:bg-white/20 transition-colors shadow-lg"
      >
        <!-- Icono cuadrado con degradado naranja-rojo -->
        <div
          class="w-20 h-20 rounded-lg
                 flex items-center justify-center mb-4"
        >
          <Image src={astro} class="w-15 h-15" alt="astro" />
        </div>

        <!-- Nombre -->
        <h3 class="text-xl font-semibold text-white mb-3">Astro.js</h3>

        <!-- Descripción -->
        <p class="text-gray-300 text-sm leading-relaxed">
          Framework ultra-rápido para sitios web estáticos y dinámicos con carga mínima de JavaScript.
        </p>
      </div>

      <!-- Tarjeta Vue.js -->
      <div
        class="flex flex-col items-center bg-white/10 backdrop-blur-md rounded-xl
               p-6 hover:bg-white/20 transition-colors shadow-lg"
      >
        <!-- Icono cuadrado con degradado verde -->
        <div
          class="w-20 h-20 rounded-lg
                 flex items-center justify-center mb-4"
        >
          <Image src={vue} class="w-15 h-15" alt="Vue" />
        </div>

        <!-- Nombre -->
        <h3 class="text-xl font-semibold text-white mb-3">Vue.js</h3>

        <!-- Descripción -->
        <p class="text-gray-300 text-sm leading-relaxed">
          Framework progresivo para interfaces de usuario reactivas y aplicaciones de una sola página.
        </p>
      </div>

      <!-- Tarjeta Next.js -->
      <div
        class="flex flex-col items-center bg-white/10 backdrop-blur-md rounded-xl
               p-6 hover:bg-white/20 transition-colors shadow-lg"
      >
        <!-- Icono cuadrado con degradado azul -->
        <div
          class="w-20 h-20 rounded-lg
                 flex items-center justify-center mb-4"
        >
          <Image src={next} class="w-15 h-15" alt="next" />
        </div>

        <!-- Nombre -->
        <h3 class="text-xl font-semibold text-white mb-3">Next.js</h3>

        <!-- Descripción -->
        <p class="text-gray-300 text-sm leading-relaxed">
          Framework de React para aplicaciones web de producción con renderizado híbrido y optimizaciones automáticas.
        </p>
      </div>
    </div>
  </div>
</section>


